# Frontend (Next.js App Router + TypeScript)

Minimal blank starter frontend for this repository.

## Local development

```bash
npm ci
npm run dev
```

App URL: `http://localhost:3000`

## Production build

```bash
npm run build
npm run start
```

## Docker

From the repo root:

```bash
docker compose up --build frontend
```
