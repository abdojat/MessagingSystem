import NotFound from "@/components/features/chat/pages/not-found";

// Renders the locale not found component; Next.js invokes it while routing and rendering the application.
export default function LocaleNotFound() {
  return <NotFound />;
}
