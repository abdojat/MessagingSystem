import { AppHomeEmptyState } from "@/components/features/chat/pages/app-layout";

// Renders the app page; Next.js invokes it while routing and rendering the application.
export default function AppPage() {
  return <AppHomeEmptyState />;
}
