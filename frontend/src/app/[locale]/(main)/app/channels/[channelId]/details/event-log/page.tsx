import ChannelEventLogPage from "@/components/features/chat/pages/channel-event-log";

// Renders the app channel event log page; Next.js invokes it while routing and rendering the application.
export default function AppChannelEventLogPage() {
  return <ChannelEventLogPage />;
}
