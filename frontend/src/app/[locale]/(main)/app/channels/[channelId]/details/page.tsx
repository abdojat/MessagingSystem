import ChannelDetailsPage from "@/components/features/chat/pages/channel-details";

// Renders the app channel details page; Next.js invokes it while routing and rendering the application.
export default function AppChannelDetailsPage() {
  return <ChannelDetailsPage />;
}
