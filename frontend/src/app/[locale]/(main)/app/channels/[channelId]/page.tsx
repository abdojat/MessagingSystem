import ChannelView from "@/components/features/chat/pages/channel-view";

// Renders the app channel page; Next.js invokes it while routing and rendering the application.
export default function AppChannelPage() {
  return <ChannelView />;
}
