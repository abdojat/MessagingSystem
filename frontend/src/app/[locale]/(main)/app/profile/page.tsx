import ProfilePage from "@/components/features/chat/pages/profile";

// Renders the app profile page; Next.js invokes it while routing and rendering the application.
export default function AppProfilePage() {
  return <ProfilePage />;
}
