import Home from "@/components/features/chat/pages/home";

// Renders the main home page; Next.js invokes it while routing and rendering the application.
export default function MainHomePage() {
  return <Home />;
}
