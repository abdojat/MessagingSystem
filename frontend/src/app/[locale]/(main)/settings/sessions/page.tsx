import Sessions from "@/components/features/chat/pages/sessions";

// Renders the sessions page; Next.js invokes it while routing and rendering the application.
export default function SessionsPage() {
  return <Sessions />;
}
