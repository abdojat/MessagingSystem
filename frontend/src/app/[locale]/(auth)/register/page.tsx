import Register from "@/components/features/chat/pages/register";

// Renders the register page; Next.js invokes it while routing and rendering the application.
export default function RegisterPage() {
  return <Register />;
}
