import Invite from "@/components/features/chat/pages/invite";

// Renders the invite page; Next.js invokes it while routing and rendering the application.
export default function InvitePage() {
  return <Invite />;
}
