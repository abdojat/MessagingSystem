import Login from "@/components/features/chat/pages/login";

// Renders the login page; Next.js invokes it while routing and rendering the application.
export default function LoginPage() {
  return <Login />;
}
